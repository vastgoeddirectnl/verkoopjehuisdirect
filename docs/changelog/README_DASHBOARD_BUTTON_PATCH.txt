# Dashboard knop — nieuwe klant invoeren

Deze patch vervangt:

- app/admin/page.jsx

Toegevoegd:
- duidelijke knop `+ Nieuwe klant invoeren` bovenaan het dashboard;
- extra actiebalk voor telefonische/WhatsApp-aanvragen;
- knop `+ Nieuwe klant` boven het lead-overzicht;
- link naar `/admin/nieuwe-lead`.

Let op:
Deze patch vervangt het dashboardbestand. De handmatige invoerfunctie zelf blijft staan op `/admin/nieuwe-lead`.
